func GetLangString(num) {
  if(num==1) return( "%s is not fixed at the other end.");
  if(num==2) return( "%s disconnected|from %s.");/*
  if(num==4) return( "Connection not possible.");
  if(num==5) return( "%s disconnected.");
  if(num==6) return( "%s cannot be connected|to %s.");
  if(num==7) return( "%s conntected|to %s");
  if(num==8) return( "Cannot create|a new line here.");
  if(num==9) return( "New|%s.");*/
  return("*blablub*");
}